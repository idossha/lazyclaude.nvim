# Personal Preferences

- I prefer concise responses. Skip preamble.
- Use git conventional commits: `feat:`, `fix:`, `refactor:`, `docs:`, `test:`
- When writing TypeScript, use strict mode and avoid `any`
- For Python, follow PEP 8 and use type hints
- I use neovim, so suggest vim-friendly workflows when relevant
